# Dub 本地初始化账号

## 账号角色

| 账号                            | 角色      | 主要用途                        |
| ------------------------------- | --------- | ------------------------------- |
| `owner@dub-internal-test.com`   | `owner`   | workspace 所有者，权限最高      |
| `member@dub-internal-test.com`  | `member`  | 普通成员，可以管理常规业务数据  |
| `viewer@dub-internal-test.com`  | `viewer`  | 只读用户，只能查看              |
| `billing@dub-internal-test.com` | `billing` | 财务用户，主要管理账单和 payout |

## 密码

password

## 权限说明

| 权限                                                | owner | member | viewer | billing |
| --------------------------------------------------- | ----- | ------ | ------ | ------- |
| 查看 links / analytics / workspace / domains / tags | 可以  | 可以   | 可以   | 可以    |
| 管理 links                                          | 可以  | 可以   | 不可以 | 不可以  |
| 管理 tags                                           | 可以  | 可以   | 不可以 | 不可以  |
| 管理 folders                                        | 可以  | 可以   | 不可以 | 不可以  |
| 管理 groups                                         | 可以  | 可以   | 不可以 | 不可以  |
| 管理 messages                                       | 可以  | 可以   | 不可以 | 不可以  |
| 管理 integrations                                   | 可以  | 可以   | 不可以 | 不可以  |
| 管理 workspace settings                             | 可以  | 不可以 | 不可以 | 不可以  |
| 管理 domains                                        | 可以  | 不可以 | 不可以 | 不可以  |
| 管理 API keys / tokens                              | 可以  | 不可以 | 不可以 | 不可以  |
| 管理 OAuth apps                                     | 可以  | 不可以 | 不可以 | 不可以  |
| 管理 webhooks                                       | 可以  | 不可以 | 不可以 | 不可以  |
| 管理 billing                                        | 可以  | 不可以 | 不可以 | 可以    |
| confirm payouts                                     | 可以  | 不可以 | 不可以 | 可以    |

## 简单理解

| 角色      | 含义           |
| --------- | -------------- |
| `owner`   | 管理员         |
| `member`  | 普通可编辑成员 |
| `viewer`  | 只读成员       |
| `billing` | 财务/账单成员  |
